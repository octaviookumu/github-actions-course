name: Webhook Event

on:
  repository_dispatch:
    types: [webhook-event]

jobs:
  respond-to-dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Run a script
        run: echo "Event of type: $GITHUB_EVENT_NAME"